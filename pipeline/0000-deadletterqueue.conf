input {
  dead_letter_queue {
    path => "/data/log_collector/data/dead_letter_queue" 
    commit_offsets => true 
    pipeline_id => "telemetry"
    type => "dead_letter_queue"
    codec => "json"
  }
}
